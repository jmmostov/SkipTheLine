<!DOCTYPE html>

<html lang="en">
<%- include('layouts/header.ejs');-%>
<body>
<%- include('layouts/navbar.ejs');-%>

<section class="get">
    <h3> Orders available for pick-up</h3>
    <div>
        <% for(var i = 0; i < order.length; i++) { %>
            <article class="data">
                <hr>
                <div> <h3>Delivery Location:</h3> <%= order[i].deliveryLocation %> </div>
                <br>
                <% let arr = [];
                for (let id in order[i].lineItem.items) {
                        arr.push(order[i].lineItem.items[id]);
                    }
                for(var u = 0; u < arr.length; u++) { %>
                    <hr>
                    <div> <b>Product:</b> <%= arr[u].product.productName %> <br>
                    <b>Quantity:</b> <%= arr[u].qty %></div>
                    <br>

            </article>
            <% } %>
            <button type="button">Take care of this delivery</button>
        <% } %>
    </div>
</section>

<!--
<section class="update">
    <h3>UPDATE data</h3>
    <form id="updateForm" name="updateForm" action="/update/:id" enctype="application/x-www-form-urlencoded">
        <div class="input">
            <label for="id"> ID </label>
            <input type="text" id="id" name="id">
        </div>
        <br>
        <div class="input">
            <label for="username"> Username </label>
            <input type="text" id="updateUsername" name="updateUsername">
        </div>
        <br>
        <button type="submit"> Update</button>
    </form>
</section>
-->

<%- include('layouts/scripts');-%>
</body>
</html>