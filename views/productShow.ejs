<!DOCTYPE html>

<html lang="en">
<%- include('layouts/header.ejs');-%>

<body>
<%- include('layouts/navbar.ejs');-%>


<!-- Main Content -->
<div class="container">
    <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
            <!-- We create a for loop to loop through all our products in the database.
            Next, we find the name of the product and the price of the product. -->
            <% for(var i = 0; i < products.length; i++) { %>
                <div class="post-preview">
                    <h2 class="post-title">
                        <%= products[i].productName %>
                    </h2>
                    <h3 class="post-subtitle">
                        <%= products[i].price + ",-"%>
                    </h3>
                </div>
                <div class="form-group">
                    <!-- We create a button, "Add to cart", which is a link to /lineItem/:id with the relevant id -->
                    <a href="/lineItem/<%= products[i]._id%>" class="btn btn-primary" role="button">Add to cart</a>
                </div>
            <% } %>
        </div>
    </div>
</div>


<%- include('layouts/scripts');-%>
</body>

</html>